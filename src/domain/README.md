# 案件モデル

`CONTEXT.md` でいう**案件**の状態と、それを進める操作をここに置く。

このモジュールは純粋な TypeScript とする。DOM・ブラウザの API・ネットワーク・
ファイルのいずれにも依存させない。画面はここを呼ぶだけで、ここを介さずに
案件の状態を書き換えない。

検証のルール（何枚揃えば次へ進めるか、いつ作成完了を押せるか）は、
すべてこのモジュールだけが持つ。画面側に同じ判定を書き足さない。

中身は Issue #4（顧客情報の入力）から順に育てる。
